<div class="container">

  <ul class="nav nav-lb-tab mb-6 justify-content-center" id="tab" role="tablist">
    <li class="nav-item ms-0" role="presentation">
      <a class="nav-link <%= active_if('events#index') %>" href="/" role="tab">新着イベント</a>
    </li>
    <li class="nav-item" role="presentation">
      <a class="nav-link <%= active_if('events#future') %>" href="/events/future" role="tab">直近イベント</a>
    </li>
    <li class="nav-item" role="presentation">
      <a class="nav-link <%= active_if('events#past') %>" href="/events/past" role="tab">過去イベント</a>
    </li>
  </ul>

  <div class="row row-cols-1 row-cols-md-3 g-4 mb-3">
  
  <%# 以下女性には女性と男子両方のイベント、それ以外は表示なし　%>
  
 <% if current_user.gender == 'woman' %>
 <%= render @events.only_woman && @events.not_only_woman %>
 <% else %>
 <%= render @events.not_only_woman %>
 <% end %>
    
   <%# 自分　%>
    
  </div>
  <div class="mx-auto">
    <%= paginate @events %>
  </div>
</div>
